export {
    radios,
    buttons,
    events
}

const events = {
    on: 'on_evt',
    off: 'off_evt'
}

const radios = {
    on: 'on_rad',
    off: 'off_rad',
    group: 'g_rad'
}

const buttons = {
    on: 'on_btn',
    off: 'off_btn'
}